<Page
    x:Class="UniAli.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UniAli"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Область вывода ответа модели -->
        <ScrollViewer Grid.Row="0" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="ResponseTextBlock"
                      Text="Добро пожаловать в UniAli! Задайте вопрос..."
                      TextWrapping="Wrap"
                      Margin="20"
                      FontSize="16" />
        </ScrollViewer>

        <!-- Панель ввода -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20" VerticalAlignment="Center">
            <ScrollViewer Width="300" Height="200" Margin="0,0,10,0">
                <TextBox x:Name="InputTextBox"
                 PlaceholderText="Введите ваш вопрос..."
                 AcceptsReturn="True"
                 MinHeight="200"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 KeyDown="InputTextBox_KeyDown"/>
            </ScrollViewer>
            <Button Content="Отправить"
            Width="100"
            Height="40"
            Click="SendButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
